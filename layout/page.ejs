<div class="container mx-auto">
  <% if (page.type === 'about') {%>
  <p><%- page.content %></p>
  <%}%> <% if (page.type === 'tags') { %>
  <h3><%= _p('counter.tags', site.tags.length) %></h3>
  <%- tagcloud({min_font: 18, max_font: 28, amount: 30, orderby: 'count'}) %> <%
  } %>
</div>
